# PLNM 项目 — AI 辅助系统笔记

## 一、这个 AI 辅助系统是干什么的？

简单说：**你在聊天框里打字，AI 帮你做事或回答问题。**

它不是一个"什么都能聊"的通用聊天机器人，而是一个**带着身份（角色）来干活的 AI 助手**。不同角色有不同分工，有的帮你执行操作，有的帮你查笔记，有的帮你分析消费。

---

## 二、5 个 AI 角色——各有各的活

想象你有一个团队，5 个人各司其职：

| 角色 | 相当于什么人 | 工作方式 | 能做什么 |
|------|-------------|---------|---------|
| **系统操作员** | 执行秘书 | 你说一句，它立刻做，不等AI慢慢想 | 帮你新建笔记、记一笔消费、更新学习进度、新增学习资源；还能**回滚**上一步操作 |
| **笔记助手** | 笔记管理员 | 一边想一边说（流式输出），从你的笔记库里找答案 | 你问问题，它从你已有的笔记里搜索、总结、引用原文 |
| **生活中心管家** | 理财顾问 | 流式输出，从你的消费数据里分析 | 告诉你这个月花了多少、哪类花得多、给出3条建议 |
| **学习中心导师** | 学习导师 | 流式输出，从你的学习进度里分析 | 告诉你平均进度、哪些课薄弱、给3步学习路径 |
| **创作搭档** | 头脑风暴伙伴 | 流式输出，不依赖任何业务数据 | 帮你发散思维、出创意、讨论想法 |

### "流式"和"非流式"是什么意思？

- **非流式**（系统操作员）：你说完，它想好了再一次性回复给你。就像发邮件，等对方写完才能看到。
- **流式**（其他4个角色）：它一边想一边把字"吐"出来，你能看到回复一个字一个字冒出来。就像面对面说话，说一句听一句。

---

## 三、整个流程是怎么跑起来的？（从你打字到看到回复）

```
你输入文字
   |
   v
前端（AiChat.vue）把消息发给后端
   |
   +--> 如果角色是非流式的 → 调 /api/ai/chat → 一次性返回结果
   |
   +--> 如果角色是流式的 → 两条路可选：
        |
        +--> SSE 通道：调 /api/ai/stream → 像开了一个水管，数据一点一点流过来
        |
        +--> WebSocket 通道：连 ws://.../ws/ai/chat → 双向管道，更实时
```

### 3.1 系统操作员的工作流程（最复杂也最有趣）

你输入一句话，比如"记一笔餐饮消费 32 元"：

```
第1步：先问AI——"这句话想干什么？"
       → AI返回JSON：{"intent":"CREATE_EXPENSE","args":{"amount":32,"category":"餐饮"}}

第2步：如果AI没返回（比如AI没配好），就走规则匹配
       → 看到"消费""记账"→ 判断是CREATE_EXPENSE
       → 用正则从文本里提取数字32
       → 从文本里匹配"餐饮"关键词

第3步：执行操作
       → 在数据库里创建一条消费记录
       → 记到"操作日志"里（为回滚做准备）

第4步：回复你
       → "已为你记录消费：32 元"
```

**回滚**是系统操作员的特色功能：如果你发现记错了，说"回滚"，它就把刚才那条记录删掉。

### 3.2 知识型角色的工作流程（笔记助手为例）

你问"我之前关于Vue的笔记写了什么？"：

```
第1步：从数据库取出你所有的笔记（最多400条）

第2步：用关键词匹配排序
       → 把你的问题拆成词（"Vue"）
       → 笔记标题包含"Vue"的+3分，内容包含的+1分
       → 取得分最高的前3条笔记

第3步：拼成一段 Prompt（给AI的"指令+资料"）
       → "你是笔记助手，只能依据用户笔记回答"
       → "用户问题：我之前关于Vue的笔记写了什么？"
       → "可用笔记片段：笔记#5《Vue学习》... 笔记#12《Vue3新特性》..."

第4步：发给AI模型，让它基于这些笔记回答
       → 如果AI没配好，就用"兜底回复"（把匹配到的笔记内容摘要拼一下）

第5步：回复你，附带"引用"信息
       → "引用：笔记#5《Vue学习》"
```

生活中心管家和学习中心导师的逻辑类似，只是数据来源不同（一个查消费记录，一个查学习进度）。

---

## 四、两种通信方式：SSE vs WebSocket

前端可以选择用哪种方式和后端"实时对话"：

### SSE（Server-Sent Events）
- **方向**：单向——只有服务器往客户端推数据
- **原理**：浏览器发一个 HTTP 请求，服务器保持连接不断，有数据就推过来
- **类比**：像看电视——节目单向播放，你只能看

### WebSocket
- **方向**：双向——客户端和服务器都能主动发消息
- **原理**：建立连接后，两边随时可以互发
- **类比**：像打电话——双方都能说话
- **额外能力**：支持**断点续传**——如果连接断了，重连后告诉服务器"我上次收到第100个字，从101开始继续发"

前端有个开关让用户在 SSE 和 WebSocket 之间切换。

---

## 五、AI 模型是怎么接的？

后端通过 **Spring AI** 框架接入大模型（配置的是 OpenAI 兼容接口，实际可以接 GLM、GPT、Qwen 等）：

```yaml
# application.yml 里的配置
spring.ai.openai:
  base-url: https://aihubmix.com    # API 地址
  api-key: sk-xxxxx                  # 密钥
```

后端代码里通过 `OpenAiChatModel` 这个 Spring 组件来调用 AI：
- 非流式调用：`openAiChatModel.call(prompt)` → 等AI说完，一次性拿到
- 流式调用：`openAiChatModel.stream(prompt)` → 拿到一个 Flux（数据流），AI每吐出一个字就推给你

**如果 AI 没配好**（模型不可用），系统不会崩，而是走"兜底回复"——用代码逻辑拼一个基本能用的答案。这就是**优雅降级**。

---

## 六、审计日志——AI 做了什么，全都有记录

每次 AI 交互都会记录一条审计日志，存在 `ai_audit_log` 表里：

| 字段 | 意思 |
|------|------|
| user_id | 谁在用 |
| role_id | 哪个角色在干活 |
| action_type | 做了什么类型的事（CREATE_NOTE、NOTE_QA 等） |
| module | 哪个业务模块（system/notes/life/learning） |
| request_text | 用户说了什么 |
| response_text | AI 回了什么 |
| success | 成功还是失败 |
| latency_ms | 耗时多少毫秒 |
| trace_id | 追踪ID，可以串联整条链路 |

前端有个审计日志页面（`/ai/audit`），可以按角色、时间、关键词筛选查看。

日志默认保留 **90 天**，之后自动清理。

---

## 七、跨角色引用——角色之间可以"串门"

开关闭合时，各角色各干各的，互不干扰。打开"跨角色引用"后：

- 发消息时，前端会把**其他角色最近的回复摘要**一起传给后端
- 比如你在"学习中心导师"里问问题，同时会把"生活中心管家"最近的结论也带过去
- AI 就能参考其他角色的分析结果，给出更综合的建议

---

## 八、安全设计

1. **用户隔离**：所有查询都带 `userId`，A 用户绝对看不到 B 用户的数据
2. **权限校验**：每个请求都要验 JWT token，没登录就直接拒绝
3. **操作可回滚**：系统操作员的每次操作都记了日志，说"回滚"就能撤
4. **审计留痕**：AI 每次对话都有记录可查
5. **密钥安全**：API Key 通过环境变量注入，不硬编码（虽然目前 yml 里写了明文，这是待改进的点）

---

## 九、整体架构一图流

```
┌─────────────────────────────────────────────────────────┐
│                    Vue 前端 (AiChat.vue)                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │角色标签栏│  │对话列表  │  │消息面板  │  │输入区    │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│     SSE ←→ WebSocket                  语音输入          │
└─────────────────────┬───────────────────────────────────┘
                      │ HTTP / WS
┌─────────────────────▼───────────────────────────────────┐
│               Spring Boot 后端                           │
│  ┌──────────────────┐  ┌─────────────────────────────┐  │
│  │ AiQuickAction    │  │ AiStreamService (SSE流式)    │  │
│  │ Controller       │  │ AiChatWebSocketHandler (WS)  │  │
│  │ /api/ai/*        │  │ /ws/ai/chat                  │  │
│  └────────┬─────────┘  └──────────────┬──────────────┘  │
│           │                           │                  │
│  ┌────────▼───────────────────────────▼──────────────┐  │
│  │          AiQuickActionService (核心逻辑)           │  │
│  │  ┌─────────────┐  ┌──────────────┐                │  │
│  │  │ parseByAi() │  │ parseByRule()│ 意图解析       │  │
│  │  └─────────────┘  └──────────────┘                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │ handleSystemOperator / handleNoteAssistant  │  │  │
│  │  │ handleLifeHubButler / handleLearningMentor  │  │  │
│  │  │ handleGenericCreativeRole                   │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └──────────┬──────────────────┬────────────────────┘  │
│             │                  │                        │
│  ┌──────────▼──────┐  ┌───────▼──────────┐            │
│  │ AiRoleConfig    │  │ OpenAiChatModel   │            │
│  │ Service(角色配置)│  │ (AI大模型调用)    │            │
│  └─────────────────┘  └──────────────────┘            │
│             │                                          │
│  ┌──────────▼──────────────────────────────────────┐  │
│  │ AiAuditLogService → ai_audit_log 表（审计日志） │  │
│  └─────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────┐  │
│  │ AiStreamCacheService（内存缓存，支持断点续传）   │  │
│  └─────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
             │
    ┌────────▼────────┐
    │   MySQL 数据库   │
    │  ai_audit_log   │
    │  + 业务表        │
    └─────────────────┘
```

---

## 十、关键文件速查表

| 想了解什么 | 看哪个文件 |
|-----------|-----------|
| 前端 AI 聊天界面 | `vue/src/views/manager/ai/AiChat.vue` |
| 前端审计日志页面 | `vue/src/views/manager/ai/AiAuditLog.vue` |
| 前端 AI 相关 API 调用 | `vue/src/api/index.js`（搜 `aiApi`） |
| 后端 AI 接口入口 | `controller/AiQuickActionController.java` |
| 后端核心业务逻辑 | `service/AiQuickActionService.java` |
| 后端 SSE 流式服务 | `service/AiStreamService.java` |
| 后端 WebSocket 处理 | `config/AiChatWebSocketHandler.java` |
| 后端角色配置 | `service/AiRoleConfigService.java` + `resources/ai/roles.yml` |
| 后端审计日志 | `service/AiAuditLogService.java` + `entity/AiAuditLog.java` |
| 后端流式缓存（断点续传） | `service/AiStreamCacheService.java` |
| 数据库表结构 | `docs/ai-multi-role/schema.sql` |
| 操作手册 | `docs/ai-multi-role/README.md` |
